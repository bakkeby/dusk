static Picture geticonprop(Window w, unsigned int *icw, unsigned int *ich);
static void freeicon(Client *c);
static void updateicon(Client *c);
static int load_icon_from_png_image(Client *c, const char *iconpath);
